<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#000000"
    android:orientation="vertical"
    android:paddingStart="48dp"
    android:paddingTop="36dp"
    android:paddingEnd="48dp"
    android:paddingBottom="32dp">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-light"
        android:text="@string/diagnostics_title"
        android:textColor="#FFFFFFFF"
        android:textSize="34sp" />

    <ScrollView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_marginTop="18dp"
        android:layout_weight="1"
        android:fillViewport="false">

        <TextView
            android:id="@+id/diagnostics_text"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="monospace"
            android:lineSpacingExtra="3dp"
            android:textColor="#CCFFFFFF"
            android:textSize="13sp" />
    </ScrollView>

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="18dp"
        android:orientation="horizontal">

        <Button
            android:id="@+id/copy_diagnostics_button"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:focusable="true"
            android:text="@string/copy_diagnostics"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/restart_discovery_button"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:layout_marginStart="12dp"
            android:focusable="true"
            android:text="@string/restart_discovery"
            android:textAllCaps="false" />

        <Button
            android:id="@+id/export_diagnostics_button"
            android:layout_width="wrap_content"
            android:layout_height="48dp"
            android:layout_marginStart="12dp"
            android:focusable="true"
            android:text="@string/export_diagnostics"
            android:textAllCaps="false" />
    </LinearLayout>
</LinearLayout>
